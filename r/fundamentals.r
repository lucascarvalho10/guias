# comentários utilizam o '#' 
print('olá')
1 + 3 * (24/2)

# para declarar variáveis
name <- 'Lucas'
age <- 20

# para declarar vetores
phone <- c(543,232,1293)

# Comparadores 
56 >= 129 # FALSE
56 != 129 # TRUE

# Uso de if's e operadores & e | e !
if (day == 'Saturday' | day == 'Sunday') {
  print('Enjoy the weekend!')
} else {
  print('Do some work.')
}

# Chamada de Funções
sort(c(2,4,10,5,1)); # Outputs c(1,2,4,5,10)
length(c(2,4,10,5,1)); # Outputs 5
sum(5,15,10) #Outputs 30
unique(c(2,4,10,5,1, 5)) #Retorna c(2,4,10,5,1)